import 'package:flutter/material.dart';

class DividerInColumn extends StatelessWidget {
  const DividerInColumn({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.grey.shade300,
      thickness: 1,
    );
  }
}
